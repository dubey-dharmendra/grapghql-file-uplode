import typedefs from "./typedef.js";
import queries from "./queries.js";
import mutations from "./mutation.js";
import resolvers from "./resolvers.js";

const users = { typedefs, queries, mutations, resolvers };

export default users;
